version: '3.8'

services:
  mind-wandering-detector:
    build: .
    container_name: mind-wandering-detector
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /dev/video0:/dev/video0  # Camera access
    devices:
      - /dev/video0:/dev/video0   # Camera device
    network_mode: host
    privileged: true
    stdin_open: true
    tty: true
    command: python3 simple_app.py

  # Alternative service for headless operation
  mind-wandering-detector-headless:
    build: .
    container_name: mind-wandering-detector-headless
    volumes:
      - /dev/video0:/dev/video0
    devices:
      - /dev/video0:/dev/video0
    ports:
      - "8080:8080"
    environment:
      - HEADLESS_MODE=true
    command: python3 simple_app.py --headless
